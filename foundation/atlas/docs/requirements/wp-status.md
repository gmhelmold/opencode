# WP Status Ledger

> Baseline: `6cc2059` (2026-09-07). Generated by `npm run wp-audit`.
> This ledger separates evidence from completion. A test reference is not a BUILT verdict.

## Current Inventory

| class | cards |
| --- | ---: |
| campaign WPs | 113 |
| remediation WPs | 24 |
| total WP IDs | 137 |

## First Audit

| status | cards | meaning |
| --- | ---: | --- |
| `EXPLICIT-BUILT` | 22 | Card file explicitly closes its campaign as built. |
| `ACCEPTANCE-EVIDENCE` | 97 | Every SCN named by card appears in current test corpus. |
| `TEST-EVIDENCE` | 6 | Partial acceptance or direct WP test reference found. |
| `REVIEWED-BUILT` | 11 | Manual focused review and targeted tests closed this evidence gap. |
| `DELEGATED-ORCHESTRA` | 1 | Contract belongs to Orchestra integration, not Atlas core production code. |
| `OPEN` | 0 | Manual review found an unimplemented or unproven contract. |
| `REVIEW` | 0 | No evidence classification remains. |

Statuses are exclusive. Current audit has **136 Atlas cards with implementation/test evidence**, **1 delegated
integration card**, and **0 OPEN cards**. Evidence-positive is not automatically BUILT; card-level cold closure
review and merged commit evidence remain required.

## Delegated Integration

- `WP-5.17.TOOLS` belongs to Orchestra. Atlas keeps opaque `EmitApi.absorb`; Orchestra defines `ResultCard`
  and wave-close driver.

Focused evidence and manual decisions live in `wp-status-overrides.json`; script validates every override
against an existing card and requires non-empty evidence.

## Closure Rule

Mark card `BUILT` only after cold review confirms source implementation, acceptance tests, held-out coverage
where applicable, all module gates, scope ownership, and a merged commit. Mark out-of-band cards separately;
do not inflate campaign completion with remediation evidence.

Run `npm run wp-audit -- --json` for per-card evidence rows.
