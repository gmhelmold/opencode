---
id: S3-derive-goldens
state: S3
version: 2.0.0
protocol_ref: ../../DECOMPOSITION-PROTOCOL.md#s3--goldens-worked-protocol-goldens  # @sha pinned at method-freeze
artifact_template: inline  # the template facet lives in the state contract (protocol_ref)
skills: [goldens, completeness]
inputs: [requirements, tagged_register, fspec_merge]
next_state: C-roadmap
---

## Role & Placement
You produce the **goldens** (state S3) — each requirement's verifiable face — by **generating** them from the
invariant's method-tag, not by hand. Stakes: a hand-authored golden is a third copy of the fact that drifts
from the requirement; and a golden with **no teeth** — one no mutant of its requirement can break — is a
vacuous false-green, the single most dangerous artifact in the pipeline (IBM field data: a trivially-passing
check *always* hid a real bug). Generate, and prove each golden **can fail**.

## Inputs
<inputs>
requirements: {{REQUIREMENTS}}
register:     {{TAGGED_REGISTER}}
model:        {{FSPEC_MERGE}}
</inputs>

## Pre-conditions
- **Load** `../../DECOMPOSITION-PROTOCOL.md` §S3 and the `goldens` + `completeness` skills — apply the generation
  ladder + Gate-3 teeth as law; do not restate.
- Requirements + method-tags frozen. Else **ABORT**.

## Failure modes to guard (what a model gets wrong *here*)
- **Hand-authoring a generated case** — writing Given-When-Then by hand for an INV that has a generator (model /
  exhaustive / PBT). Forbidden: generate the cases; only the true residue is hand-written.
- **Toothless golden** — one no mutant of its requirement breaks. Every golden must **flip to BROKEN** on the
  mutant that violates its REQ (Gate 3). A golden that survives its own mutant is vacuous.
- **Antecedent-failure vacuity** — a golden "passes" because the design never enters the state its REQ governs.
  Require an *interesting witness*: a trace that non-trivially exercises the property.
- **Happy-path only** — dropping the guards. One guard-golden per `unwanted[]` clause, not just the happy case.

## Procedure
1. Per REQ: derive `SCN`s via the `goldens` generation ladder keyed on the INV's method-tag (model-traces /
   exhaustive / PBT / conformance / residue).
2. Cover the happy path + one guard `SCN` per `unwanted[]` clause.
3. Run `completeness` Gate 3: for each `SCN` name the mutant it must break on; drop any vacuous one back to its REQ.

## Output Contract
Co-located under each REQ:
```
### SCN-<MODULE>-<n>[c]-<k> — <title>   (happy | guard)
source: REQ-<MODULE>-<n>[-c]
Given <concrete state>
When <the event>
Then <the concrete asserted outcome>
teeth: breaks-on "<the mutant that must flip this to BROKEN>"
gen: model | exhaustive | PBT | conformance | residue
```

## Examples (copy the *form*)
```
### SCN-KERNEL-10a-1 — ordered heads → tip wins   (happy)
source: REQ-KERNEL-10a
Given a nodeKey with two concurrent heads H1 ≺ H2 in the supersedes order
When the kernel merges them
Then the head is H2
teeth: breaks-on "head-rule mutated to pick H1 (the non-tip)"
gen: PBT       # core is PBT-first (merge semilattice/partial-order laws); `model` only if S2 escalated to TLC

### SCN-KERNEL-10b-1 — unordered heads → contentHash tie-break   (guard)
source: REQ-KERNEL-10b
Given two concurrent heads H1, H2 unordered by supersedes, with contentHash(H1) < contentHash(H2)
When the kernel merges them
Then the head is H2   # max-by-contentHash is the pinned-canonical direction (FSPEC-merge §UP KERNEL-10)
teeth: breaks-on "tie-break mutated to min-contentHash"
gen: PBT      # core is PBT-first; `model` (FSPEC-merge traces) only if S2 escalated to TLC

### SCN-RETR-6-1 — determinism under capacity   (happy)   # a different INV + a different generator
source: REQ-RETR-6
Given a retrieval set of 10 items with retention scores and a capacity of 6
When the drop-policy runs twice on the identical input
Then both runs drop the identical 4 items in the identical order
teeth: breaks-on "drop-policy mutated to break a tie by insertion-order (nondeterministic)"
gen: PBT      # RETR-6 is method-tag `PBT` → cases from the PBT engine
```

## Self-Check
- [ ] every REQ ≥1 happy `SCN` + a guard `SCN` per `unwanted[]` clause (reconciler guard coverage)?
- [ ] cases **generated** from the method-tag; only true residue hand-written?
- [ ] Gate 3: every `SCN` names the mutant it breaks on; none vacuous / antecedent-failure?
- [ ] every `SCN` co-located under its REQ, `source: REQ`, concrete values (no abstractions)?

## Abstain / Failure
A REQ with no *writable* golden ⇒ it should have failed `atom-gate` *Verifiable* upstream; route it back — do
**not** fabricate a passing golden.

## Completion Report
Emit: goldens path · `SCN` count · coverage (REQ with ≥1 SCN) · teeth pass-rate (SCN with a breaking mutant) →
next_state **S4**. If any golden is toothless, **STOP**.
