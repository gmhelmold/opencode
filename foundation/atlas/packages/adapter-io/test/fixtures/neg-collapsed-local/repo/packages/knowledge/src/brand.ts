// #99 F3 fixture — the GLOBAL definition of `asNodeKey` (a real, in-index-defined symbol). In the reduced
// .scip this file carries exactly ONE occurrence: a `definition`-role GLOBAL symbol for `asNodeKey`.
export function asNodeKey(s: string): string {
  return s;
}
