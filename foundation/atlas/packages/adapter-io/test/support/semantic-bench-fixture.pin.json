{
  "fixture": "semantic-bench-fixture.json",
  "algo": "md5",
  "digest": "daa42ed88403e13b8ab1197adab9f0d3",
  "why": "AC-7 (#196b WP-1). The synthetic self-proof fixture is content-hashed (inv-text-pins discipline). semantic-bench.test.ts recomputes this digest over the committed bytes and FAILS if it diverges — so the numbers the instrument proves itself against cannot be edited without a same-commit re-pin naming the ratifier. Records what is IN THE TREE, not an owner ratification.",
  "substrate": {
    "rev": "e35dcce",
    "scip": ".atlas/index.scip generated by @sourcegraph/scip-typescript over atlas-self (NOT committed; 16MB). AC-15 reproducibility is proven by re-running the mechanical scoring over the SAME in-process substrate and asserting byte-identical per-arm {falseAdmit, recallTrue}. The tsc oracle (buildOracle) is a deterministic pure function of the checked-out src at this rev."
  }
}
